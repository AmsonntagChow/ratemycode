Source URL: https://docs.example.test/api-keys/docs
Snapshot scope: production v1

# API keys

Authenticate with `Authorization: Bearer <key>`.

Each API key expires 30 days after creation. Revoke it earlier with
`DELETE /v1/api-keys/{key_id}`; revoked keys stop authorizing new requests
immediately.
